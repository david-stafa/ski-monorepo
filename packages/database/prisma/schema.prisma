// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider        = "prisma-client"
  output          = "../generated/prisma"
  // partialIndexes: lets `@@unique` carry a `where:`, which EquipmentItem needs
  // so retiring an item frees its article number. Preview — it can change on a
  // Prisma upgrade, so re-check this index after bumping the version.
  previewFeatures = ["partialIndexes"]
}

datasource db {
  provider = "postgresql"
}
